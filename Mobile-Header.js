document.addEventListener("DOMContentLoaded", () => {
    const navCheckbox = document.querySelector("#nav-check");
    if (navCheckbox) {
      navCheckbox.checked = false; // Reset the checkbox to its default (unchecked) state
    }
  });
  